node_modules/
agent-fleet
agent-fleet.exe
*.log
.env
.env.*
!.env.example
.DS_Store
# fleet.yaml is user-specific (each maintainer's repo list).
# fleet.yaml.example is the template that ships with the repo.
fleet.yaml
